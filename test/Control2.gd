extends Window

var noteData = "res://user notes/Notes.txt"
var infoData = "res://user notes/Info.txt"
var otherData = "res://user notes/Other.txt"
	
# Called when the node enters the scene tree for the first time.
func _ready():
	return infoData
	return noteData
	#$%NoteEdit.text = noteData.get_as_text()
	#$%InfoEdit.text = infoData.get_as_text()
	#$%OtherEdit.text = otherData.get_as_text()


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
